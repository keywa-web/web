<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | Website Berita Keren</title>
  <meta name="description" content="{{ description }}">
  <style>
    body { font-family: sans-serif; line-height: 1.6; margin: 0; background-color: #f4f4f4; }
    .container { max-width: 800px; margin: auto; padding: 20px; background-color: #fff; }
    header { background: #333; color: #fff; padding: 1rem 0; text-align: center; }
    header a { color: #fff; text-decoration: none; }
    .article-list { list-style: none; padding: 0; }
    .article-item { border-bottom: 1px solid #ddd; padding: 15px 0; }
    .article-item a { text-decoration: none; color: #333; font-size: 1.5rem; }
    .article-meta { font-size: 0.9rem; color: #666; }
    .article-content img { max-width: 100%; height: auto; }
    /* Tambahkan slot untuk kode iklan di sini */
    /* {{ ad_header_script | safe }} */
  </style>
</head>
<body>
  <header>
    <h1><a href="/">Website Berita Keren</a></h1>
  </header>
  <main class="container">
    {{ content | safe }}
  </main>
</body>
</html>